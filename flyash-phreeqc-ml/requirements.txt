# Pinned-loosely runtime dependencies for Phase 1.
# Prefer `pip install -e .` (uses pyproject.toml); this file is for plain installs.
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
openpyxl>=3.1          # read/write .xlsx workbooks
xlrd>=2.0              # read legacy .xls workbooks (experimental data import)
streamlit>=1.30        # GUI: streamlit run app.py
