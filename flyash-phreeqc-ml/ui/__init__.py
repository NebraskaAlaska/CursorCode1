"""UI layer package — tab renderers + pure helpers (see docs/refactor_plan.md).

Only pure helpers (ui/formatters.py) live here for now; the _render_* tab functions
remain in app.py until the dedicated refactor PR.
"""
